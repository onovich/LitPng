import { writeFile } from "node:fs/promises";

await writeFile(
  new URL("../apps/web/src/codecs/pngquant-wasm/.gitignore", import.meta.url),
  "# Generated by `npm run codec:build-wasm` and committed for reproducible static builds.\n",
  "utf8"
);
